# 屹力学生综评项目启动与二次修改指南

本文档用于把本项目交给其他协作者，帮助其使用 Codex 完成本地启动、页面预览、二次修改、变更查看和交付检查。

## 1. 项目简介

这是一个基于 Next.js App Router 的小学学生综合素质评价前端原型，包含教师端、管理端和家长端的多角色工作台，以及班级评价、奖卡、荣誉、活动、成绩、体测、学期报告、积分商城和数据大屏等页面。

当前版本是可交互的前端演示原型：

- 身份、权限、业务数据均为前端模拟数据。
- 页面中的新增、修改、删除和任务状态通常通过浏览器 `localStorage` 持久化。
- 没有真实登录、后端接口、服务端数据库、消息推送或生产审计能力。
- 项目使用静态导出配置，应用访问前缀固定为 `/xszp`。

## 2. 环境要求

建议使用以下环境：

- Node.js 22 或更高版本
- pnpm 10 或更高版本
- Git
- Codex（在项目根目录打开）

如果本机尚未启用 pnpm，可以先执行：

```bash
corepack enable
corepack prepare pnpm@10 --activate
```

Windows PowerShell、macOS 和 Linux 均可使用下方的 pnpm 命令。若团队统一使用 npm，也可以将 `pnpm install`、`pnpm dev`、`pnpm build` 分别替换为对应的 npm 命令，但建议协作时统一包管理器。

如果 Windows PowerShell 提示 `pnpm.ps1` 被系统策略禁止执行，请将命令中的 `pnpm` 替换为 `pnpm.cmd`，例如执行 `pnpm.cmd dev` 或 `pnpm.cmd build`。

## 3. 首次启动

在项目根目录执行：

```bash
pnpm install
pnpm dev
```

然后打开：

```text
http://localhost:3000/xszp/
```

开发服务器会在保存代码后自动刷新或热更新，适合边修改边查看页面。终止服务使用 `Ctrl+C`。

如果 3000 端口已被占用，可以换端口：

```bash
pnpm dev -- --port 3001
```

访问地址对应变为：

```text
http://localhost:3001/xszp/
```

注意：不要省略 `/xszp/`。`next.config.mjs` 中设置了 `basePath: "/xszp"`，直接访问根路径可能看不到应用。

## 4. 构建检查

提交修改前至少执行一次：

```bash
pnpm build
```

静态构建产物会输出到 `out/`。当前 `package.json` 没有单独配置 lint 或 test 脚本，因此不要把不存在的测试命令当作验收依据；页面交互需要在浏览器中实际点击验证。

## 5. 用 Codex 进行二次修改

请在项目根目录启动 Codex，并让 Codex 先阅读以下文件：

1. `AGENTS.md`：项目级规则，包括产品文档同步要求。
2. `项目启动文档.md`：启动、预览和协作约定。
3. `prd/student-comprehensive-evaluation.md`：当前产品范围、角色权限、验收标准和变更记录。
4. 与任务最相关的页面、组件、数据和样式文件。

推荐使用下面的任务描述模板：

```text
请先阅读 AGENTS.md、项目启动文档.md 和 prd/student-comprehensive-evaluation.md。

本次修改目标：<用一句话描述要改什么>
涉及页面/角色：<页面路径或角色>
期望交互：<用户如何操作、页面应如何反馈>
不希望改变的内容：<列出需要保持不变的范围>

请先检查现有实现和相关数据流，再完成修改。修改后请：
1. 更新必要的产品文档和 PRD 变更记录；
2. 执行 pnpm build；
3. 汇总修改过的文件、浏览器验证路径和仍存在的限制。
```

对于纯视觉微调，也要写清楚目标页面、桌面端/移动端范围和不能改变的交互，避免 Codex 误改其他角色或页面。

## 6. 项目结构与修改入口

| 目录或文件 | 用途 | 常见修改场景 |
| --- | --- | --- |
| `app/` | Next.js 路由入口 | 新增页面、调整页面路径、页面级权限壳 |
| `components/` | 业务组件和 UI 组件 | 页面布局、交互、表格、弹窗、工作流 |
| `components/ui/` | 通用基础组件 | 按钮、输入框、弹窗、Tab、加载状态 |
| `lib/mock-data.ts` | 学校、班级、学生、教师、家长和演示记录 | 调整演示数据、角色样例、权限范围 |
| `lib/types.ts` | 领域类型定义 | 新增或调整数据字段 |
| `lib/evaluation-context.tsx` | 全局状态、模拟记录和本地存储 | 跨页面状态、保存/删除、身份切换 |
| `lib/use-permission.ts` | 当前身份的权限判断 | 角色入口和操作权限 |
| `public/` | 图片、图标和静态资源 | 更换图片、添加奖卡或首页素材 |
| `app/globals.css`、各模块 `*.module.css` | 全局和模块样式 | 响应式布局、主题、背景、断点 |
| `design-system/student-comprehensive-evaluation/MASTER.md` | 当前项目视觉与组件设计参考 | 设计方向、颜色、字体和界面规范 |
| `prd/student-comprehensive-evaluation.md` | 产品需求、范围、验收标准和变更记录 | 产品行为、权限、字段或流程发生变化时同步 |
| `next.config.mjs` | 静态导出、`/xszp` 子路径等构建配置 | 部署路径或构建方式变化 |
| `.github/workflows/deploy.yml` | GitHub Pages 自动构建部署 | 部署方式或 Node 环境变化 |

通常建议先找路由入口，再找到对应的业务组件，最后查阅 `lib/` 中的状态和数据来源。不要只修改演示数据来伪装交互已经完成。

## 7. 常用页面路径

本地开发时，所有路径都要带 `/xszp` 前缀：

| 页面 | 路径 |
| --- | --- |
| 角色工作台入口 | `/xszp/` |
| 嵌入式工作台 | `/xszp/workbench/` |
| 班级评价 | `/xszp/class-evaluation/` |
| 班级排行 | `/xszp/class-ranking/` |
| 评价配置 | `/xszp/class-evaluation-config/` |
| 奖卡发放 | `/xszp/award-cards/` |
| 活动管理 | `/xszp/activities/` |
| 成绩录入 | `/xszp/score-entry-management/` |
| 体质健康导入 | `/xszp/pe-score-import/` |
| 学期评价 | `/xszp/semester-evaluation/` |
| 积分商城 | `/xszp/points-mall/` |
| 学校数据大屏 | `/xszp/school-command-center/` |

首页右上角可以切换模拟身份。修改权限、导航或角色首页时，应至少验证管理员、班主任、任课教师和家长四类身份中与任务相关的角色。

## 8. 如何查看“修改情况”

推荐每位协作者使用独立分支：

```bash
git switch -c feat/<简短任务名>
```

修改前先确认工作区：

```bash
git status --short
```

修改过程中查看整体变化：

```bash
git diff --stat
git diff
```

只查看某个文件：

```bash
git diff -- components/<相关文件>.tsx
```

完成后建议按“一个任务一个提交”保存：

```bash
git add <本次实际修改的文件>
git commit -m "feat: <简短描述>"
```

其他人可以通过以下命令查看某个分支相对 `main` 的修改：

```bash
git diff main...feat/<简短任务名>
git log --oneline --decorate -n 10
```

如果不使用 Git 分支，至少在交付时提供：修改前后的截图、修改文件清单、`git diff --stat` 结果、启动地址和已验证的身份/页面路径。不要只发送 `out/`，因为静态产物无法方便地继续二次修改。

## 9. 浏览器验证清单

每次修改后根据任务范围执行以下检查：

- 页面可以通过 `/xszp/` 前缀正常打开。
- 与任务相关的桌面端页面可以正常显示，宽度缩小时没有横向溢出。
- 移动端断点下，底部导航、固定按钮、弹窗和表格仍可操作。
- 相关身份只能看到并操作其授权范围。
- 新增、修改、删除后，列表、统计、排行或提示信息即时更新。
- 刷新页面后需要保留的数据仍然存在；不应保留的数据能正确清除。
- 关键按钮、输入框、Tab 和弹窗具备可见焦点或可读的状态反馈。
- 任务涉及的页面和相邻页面没有出现控制台错误。
- `pnpm build` 执行成功。

原型数据持久化在当前浏览器中。如果预览结果被之前的操作污染，可以在浏览器开发者工具中清除当前站点数据后重新加载；这只会清理本机演示数据，不会修改项目源文件。

## 10. 产品文档同步规则

以下情况必须同步修改 `prd/student-comprehensive-evaluation.md`：

- 产品页面、用户流程或交互行为变化；
- 角色权限、导航入口或可写范围变化；
- 数据字段、模拟数据语义或数据展示方式变化；
- 新增或删除功能、验收标准或产品边界变化。

变更记录至少包含日期、变更摘要和关联的 FR/NFR/AC 编号。纯代码重构或不影响产品行为的文档/样式整理，可在交付说明中注明不涉及产品需求。

## 11. 交付时请一并说明

```text
修改目标：
涉及页面/角色：
主要修改文件：
启动命令：pnpm dev
预览地址：http://localhost:3000/xszp/
已验证路径：
已验证身份：
构建结果：pnpm build（成功/失败及原因）
PRD 是否同步：是/否（说明原因）
已知限制：
```

不要把 `.env.local`、token、账号信息、`node_modules`、`.next` 或 `out` 作为源代码压缩包的一部分发送给其他协作者。`.env.local` 仅供本机使用，当前项目也将环境文件和构建目录列入忽略规则。
